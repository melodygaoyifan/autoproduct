description: f-string interpolation into SQL
diff: 'diff --git a/app/db.py b/app/db.py

  --- a/app/db.py

  +++ b/app/db.py

  @@ -1,0 +1,2 @@

  +def find_user(user_id):

  +    return db.fetch_one(f"SELECT * FROM users WHERE id = {user_id}")

  '
expected:
- file_path: app/db.py
  keywords:
  - sql
  - injection
  - interpolat
  - parameter
name: 01-sql-injection
